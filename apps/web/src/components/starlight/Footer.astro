---
import LandingFooter from '../landing/LandingFooter.astro';

const path = Astro.url.pathname;
const lang = path.startsWith('/en/') ? 'en' : path.startsWith('/ja/') ? 'ja' : 'zh-CN';
---

<LandingFooter lang={lang} />
